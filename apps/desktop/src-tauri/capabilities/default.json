{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "IPC minimal pour la fenêtre principale; aucun shell ni accès fichier n'est exposé au frontend.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
